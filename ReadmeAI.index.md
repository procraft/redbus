# ReadmeAI module index

> GENERATED by `.agents/scripts/readme-ai-index.sh`; do not edit manually.
> Generate: `make readme-index`. Check: `make readme-index-check`. Lint: `make readme-lint`.

Map of all `ReadmeAI.md` files (3). Grep it for a topic before reading documents: every entry
is one line — directory, context card, `Sections:` (H2 titles) and `Also:` (sibling Markdown).
Relevant context may be owned by a sibling subtree, not only by the directory walk-up path.

- [api/scala/redbus](api/scala/redbus/ReadmeAI.md) — **Scala SDK — module context**: `api/scala/redbus` is the Scala client SDK for the bus (`"sergiusd" %% "redbus"`), published to the private maven repository. It is consumed by already-deployed services, each pinned to its own version, so every behavioural change here i... Sections: Actor runtime and release lines · Surfaces · Typed client · Inbox dedup modes · Outbox flusher invariants · Checks. Also: [README.md](api/scala/redbus/README.md).
- [internal/pkg/stream](internal/pkg/stream/ReadmeAI.md) — **Consume stream — контекст модуля**: `internal/pkg/stream` владеет протоколом обмена по двунаправленному `Consume`: отправкой батча клиенту и приёмом результата его обработки. Это единственное место, где сопоставляются запрос и ответ, поэтому все инварианты фазы «батч ↔ отв... Sections: Кто пишет в стрим · Корреляция батча и результата · Отложенный retry без расходования attempt · Дедлайн ожидания результата · Прочие инварианты · Проверки.
- [web/admin](web/admin/ReadmeAI.md) — **Admin UI — контекст модуля** Sections: Назначение · Стек и структура · HTTP и SSE · Локальный запуск и проверки · Сборка и деплой · Связанные визуальные assets. Also: [README.md](web/admin/README.md).
