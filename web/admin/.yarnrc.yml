nodeLinker: node-modules

enableTelemetry: false

# These exact versions were verified against the official registry; they are newer than Yarn's default one-day age gate.
npmPreapprovedPackages:
  - lucide-react@1.35.0
  - react-router@8.3.1
